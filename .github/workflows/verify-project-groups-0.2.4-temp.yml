name: Verify project groups 0.2.4 candidate

on:
  push:
    branches:
      - feature/project-groups-web-adapter-0.2.4

permissions:
  contents: write

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Verify candidate
        run: npm run check
      - name: Commit generated candidate artifact
        shell: bash
        run: |
          if git diff --quiet -- dist/orca-web-patches.user.js; then
            echo "generated artifact already current"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add dist/orca-web-patches.user.js
          git commit -m "build 0.2.4 candidate artifact [skip ci]"
          git push
